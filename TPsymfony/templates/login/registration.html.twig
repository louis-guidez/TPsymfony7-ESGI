{% extends 'base.html.twig' %}

{% block title %}S'enregistrer{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1>S'enregistrer</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}

        <p class="mt-3">Vous avez déjà un compte? <a href="{{ path('app_login') }}">Connectez-vous ici</a>.</p>
    </div>
{% endblock %}
